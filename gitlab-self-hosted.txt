устанавливаем docker
docker network create gitlab-network


sudo mkdir -p /srv/gitlab

docker run --detach \
  --hostname gitlab.local \
  --env GITLAB_OMNIBUS_CONFIG="external_url 'http://10.202.49.110'" \
  --publish 80:80 \
  --publish 443:443 \
  --publish 2222:22 \
  --publish 5050:5050 \
  --name gitlab \
  --restart always \
  --network gitlab-network \
  --volume /srv/gitlab/config:/etc/gitlab \
  --volume /srv/gitlab/logs:/var/log/gitlab \
  --volume gitlab-data:/var/opt/gitlab \
  --shm-size 256m \
  gitlab/gitlab-ce:18.9.7-ce.0
  
  Подтвердить пользователя
  docker exec -it gitlab gitlab-rails console
  
u = User.find_by(username: "admin240")
u.state = "active"
u.save!

регистрация раннера 
gitlab-runner register
1. URL: http://10.202.49.110 (тот url который был указан при создании контейнера)
2. executor
docker
3. default image
alpine:latest

gitlab-runner run










(КОНФИГ РАННЕРА С КОТОРЫМ Я ЗАПУСКАЛ САЙТ ЧЕРЕЗ ДОКЕР КОНТЕЙНЕР ЧЕРЕЗ CI/CD)

concurrent = 1
check_interval = 0
connection_max_age = "15m0s"
shutdown_timeout = 0

[session_server]
  session_timeout = 1800

[[runners]]
  name = "debian"
  url = "http://10.202.49.110/"
  id = 12
  token = "glrtr-5vcjjE6I8spqFcCMfiJElG86MQpwOjYKdDozCw.01.120ydwa2l"
  token_obtained_at = 2026-05-25T04:18:05Z
  token_expires_at = 0001-01-01T00:00:00Z
  executor = "docker"
  [runners.cache]
    MaxUploadedArchiveSize = 0
    [runners.cache.s3]
      AssumeRoleMaxConcurrency = 0
    [runners.cache.gcs]
    [runners.cache.azure]
  [runners.docker]
    tls_verify = false
    image = "alpine:latest"
    privileged = true
    disable_entrypoint_overwrite = false
    oom_kill_disable = false
    disable_cache = false
    volumes = ["/cache", "/var/run/docker.sock:/var/run/docker.sock"]
    volume_keep = false
    shm_size = 0
    network_mtu = 0

