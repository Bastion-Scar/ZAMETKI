НА ВМ

su - 
visudo
имя ALL (ALL:ALL) ALL

sudo apt install openssh-server
sudo systemctl enable ssh
sudo systemctl start ssh

sudo nano /etc/ssh/sshd_config
Пишем 
PermitRootLogin yes
PasswordAuthentication yes

sudo systemctl restart sshd

На СЕРВЕРЕ 
mkdir -p ~/.ssh
chmod 700 ~/.ssh
ssh-keygen -t rsa -b 4096 -f ~/.ssh/proxmox_vm_keys -C "proxmox_to_vm"
ssh-copy-id -i ~/.ssh/proxmox_vm_keys.pub root@IPВМ


НА СЕРВЕРЕ 
желательно скопировать ключи
cp ~/.ssh/proxmox_vm_keys
cp ~/.ssh/proxmox_vm_keys.pub 
